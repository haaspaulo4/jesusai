const { searchVerses, getVerseCount, loadVerses, buildIndex, KnowledgeStore } = require('../knowledge/store');

module.exports = {
  searchVerses,
  getVerseCount,
  loadVerses,
  buildIndex,
  KnowledgeStore,
};