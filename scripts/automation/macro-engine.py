"# -*- coding: utf-8 -*-\nimport sys\nimport json\nimport time\nimport os\n\n# Parâmetros: JSON string contendo lista de ações\n# Formato do JSON: [{\"action\": \"click\", \"x\": 100, \"y\": 200, \"color_check\": {\"x\": 100, \"y\": 200, \"hex\": \"#FFFFFF\", \"tolerance\": 10}}, ...]\nactions_payload = sys.argv[1]\n\nIS_WINDOWS = sys.platform == 'win32'\nHAS_DISPLAY = IS_WINDOWS or ('DISPLAY' in os.environ)\n\ndef verify_pixel_color(x, y, expected_hex, tolerance=10):\n    try:\n        from PIL import ImageGrab\n        # Captura 1x1 pixel no ponto indicado\n        img = ImageGrab.grab(bbox=(x, y, x+1, y+1))\n        rgb = img.getpixel((0, 0))\n        \n        # Converte Hex esperado para RGB\n        hex_clean = expected_hex.lstrip('#')\n        exp_rgb = tuple(int(hex_clean[i:i+2], 16) for i in (0, 2, 4))\n        \n        # Verifica desvio Euclidiano\n        dist = ((rgb[0]-exp_rgb[0])**2 + (rgb[1]-exp_rgb[1])**2 + (rgb[2]-exp_rgb[2])**2)**0.5\n        return dist <= tolerance, rgb\n    except Exception as e:\n        return False, str(e)\n\ndef execute_low_level_key_windows(vk_code):\n    if not IS_WINDOWS:\n        return False\n    try:\n        import ctypes\n        user32 = ctypes.windll.user32\n        # KEYEVENTF_KEYDOWN = 0\n        # KEYEVENTF_KEYUP = 2\n        user32.keybd_event(vk_code, 0, 0, 0)\n        time.sleep(0.05)\n        user32.keybd_event(vk_code, 0, 2, 0)\n        return True\n    except:\n        return False\n\n# Execução principal\ntry:\n    if not HAS_DISPLAY:\n        print(json.dumps({\"ok\": False, \"error\": \"Execução de macros PyAutoGUI/GDI exige display visível (não suportado em Linux VPS Headless puro). Utilize o driver CDP de navegador em vez disso.\"}))\n        sys.exit(0)\n\n    import pyautogui\n    pyautogui.FAILSAFE = False # Evita travas em cantos de monitor\n    \n    actions = json.loads(actions_payload)\n    results = []\n\n    for i, step in enumerate(actions):\n        act_type = step.get('action')\n        x = step.get('x')\n        y = step.get(
<truncated 2300 bytes>