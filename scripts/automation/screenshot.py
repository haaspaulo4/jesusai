"# -*- coding: utf-8 -*-\nimport sys\nimport json\nimport os\n\noutput_path = sys.argv[1]\n\nIS_WINDOWS = sys.platform == 'win32'\n\ntry:\n    screenshot = None\n    width = 1920\n    height = 1080\n    headless = False\n\n    # Detecção e Captura\n    try:\n        from PIL import ImageGrab\n        # Captura toda a tela no Windows\n        if IS_WINDOWS:\n            screenshot = ImageGrab.grab(all_screens=True)\n        else:\n            # Em Linux VPS Headless, se não houver display X11, grab() lançará exceção\n            if 'DISPLAY' not in os.environ:\n                raise Exception(\"Ambiente headless sem DISPLAY X11 configurado.\")\n            screenshot = ImageGrab.grab()\n            \n        if screenshot:\n            screenshot.save(output_path, \"JPEG\", quality=80)\n            width, height = screenshot.size\n    except Exception as e:\n        headless = True\n        # Emite flag indicando que está em ambiente headless\n        # O backend Node fará a captura usando WebSocket Chrome CDP\n        pass\n\n    text_grid = []\n    \n    # Processa OCR apenas se capturou a tela física e o Tesseract está instalado\n    if screenshot and not headless:\n        try:\n            import pytesseract\n            \n            # Configuração do caminho do Tesseract se estiver no Windows\n            if IS_WINDOWS:\n                # Caminhos padrão do Tesseract no Windows\n                tess_paths = [\n                    r\"C:\\Program Files\\Tesseract-OCR\\tesseract.exe\",\n                    r\"C:\\Program Files (x86)\\Tesseract-OCR\\tesseract.exe\",\n                    os.path.join(os.environ.get(\"LOCALAPPDATA\", \"\"), \"Tesseract-OCR\", \"tesseract.exe\")\n                ]\n                for p in tess_paths:\n                    if os.path.exists(p):\n                        pytesseract.pytesseract.tesseract_cmd = p\n                        break\n            \n            # Executa OCR mapeando bounding boxes de palavras\n            data = pytesseract.image_to_data(screen
<truncated 955 bytes>